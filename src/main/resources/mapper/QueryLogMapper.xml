<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.repository.mybatis.QueryLogMapper">

    <insert id="insertQueryLog" parameterType="com.example.demo.domain.QueryLog">
        INSERT INTO T_QUERY_LOG (
            mapper_id,
            executed_query,
            execution_time_ms
        ) VALUES (
            #{mapperId},
            #{executedQuery},
            #{executionTimeMs}
        )
    </insert>
    
    <select id="selectLatestQueryLogByMapperId" parameterType="String" resultType="com.example.demo.domain.QueryLog">
        SELECT 
            id,
            mapper_id, 
            executed_query, 
            execution_time_ms, 
            created_at 
        FROM 
            T_QUERY_LOG
        WHERE 
            mapper_id = #{mapperId}
        ORDER BY 
            created_at DESC
        LIMIT 1
    </select>
</mapper>